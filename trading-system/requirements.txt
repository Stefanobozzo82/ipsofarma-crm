# --- Core / condivise ---------------------------------------------------
pandas>=2.2,<3.0
numpy>=1.26,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0
SQLAlchemy>=2.0,<3.0

# --- Modulo 1: Data ingestion --------------------------------------------
yfinance>=0.2.40          # azioni/ETF, dati pubblici senza API key
ccxt>=4.3                 # crypto (Binance, Kraken, ...), OHLCV pubblici senza API key
# alpha_vantage>=2.3       # opzionale: sorgente dati alternativa (richiede API key) — decommentare quando serve
# polygon-api-client>=1.14 # opzionale: sorgente dati alternativa (richiede API key) — decommentare quando serve

# --- Modulo 5: Backtesting -----------------------------------------------
# Il motore di default (trading_system.backtesting) è custom: orchestra le
# stesse istanze di strategy engine e risk manager che operano dal vivo,
# invece di reimplementare le strategie in un motore esterno — vedi il
# README per il perché. backtrader/vectorbt restano qui come opzione per un
# motore indipendente da affiancare in futuro (es. per confrontare
# risultati), non sono una dipendenza richiesta dal modulo 5 così com'è.
# backtrader>=1.9.78.123
# vectorbt>=0.26

# --- Modulo 6: Execution (broker/exchange) -------------------------------
alpaca-py>=0.28            # broker live azioni/ETF (trading_system.execution.live.alpaca_broker)
# ib_insync>=0.9.86         # alternativa Interactive Brokers — decommentare se scelta questa via
# crypto live via ccxt: usa lo stesso pacchetto ccxt già richiesto dal modulo 1, in modalità autenticata

# --- Modulo 7: Dashboard/API ----------------------------------------------
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
APScheduler>=3.10,<4.0

# --- Sviluppo / test -------------------------------------------------------
pytest>=8.2,<9.0
pytest-mock>=3.14,<4.0
httpx>=0.27,<1.0
